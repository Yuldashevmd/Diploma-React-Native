import { SafeAreaView, Text } from "react-native";
import { Container } from "../../../shared/styles/global";

export const SearchScreenItem = ({ navigation }) => {
  return (
    <Container>
      <SafeAreaView>
        <Text>SearchScreenItem</Text>
      </SafeAreaView>
    </Container>
  );
};
